import styles from './user.module.css'
import { Row,Col } from 'react-bootstrap'
const UserDetail = ()=>{
    return(
        <Col className={`${styles.userContainer}`}>this is user detail page</Col>
    )
}
export default UserDetail